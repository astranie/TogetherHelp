using System;
using System.Collections.Generic;
using System.Text;
using System.Drawing;
using System.Drawing.Drawing2D;
using System.IO;
using System.Drawing.Imaging;

namespace SRC
{
    public class CreateCaptcha
    {
        //public static byte[] MakeCaptcha(out string value)
        //    //out参数作为输出 存储
        //{
        //    //Bitmap被转移到System.Drawing.Common
        //    Bitmap bitmap = new Bitmap(60, 24);
        //    //Graphics 展示图形的方式
        //    Graphics graphics = Graphics.FromImage(bitmap);
        //    FontFamily fontFamily = new FontFamily("Times New Roman");
        //    Font font = new Font(fontFamily, 16, (FontStyle.Bold | FontStyle.Italic));
        //    LinearGradientBrush brush = new LinearGradientBrush(new Rectangle(0, 0, bitmap.Width, bitmap.Height),
        //        Color.Brown, Color.Green, 1.5f,true);
        //    value = new Random().Next(999, 10000).ToString();
        //    graphics.DrawString(value, font, brush, 3, 2);

        //    MemoryStream stream = new MemoryStream();
        //    bitmap.Save(stream,ImageFormat.Jpeg);
        //    return stream.ToArray();
        //}
      


    }
}
